# Audit UI, cohérence et signaux « IA » — 31 août 2026

Périmètre : `public/themes/souverainete-digitale/index.fr.html`, `css/custom.css`, `css/hallmark-redesign.css`, `css/hallmark-tokens.css`, gabarits `content/*.fr.html`, pages FR servies en local (`:8090`). Méthode : `hallmark audit` (liste des tells de `anti-patterns.md`) + lecture des copies + rendu 1440 px et 375 px.

## État du projet

- Branche `codex/french-growth-foundation` : 13 commits devant `master`, 0 derrière, **non poussée sur origin**.
- 30 fichiers non commités : le plan `docs/superpowers/plans/2026-08-27-ui-polish-anti-slop.md` a été exécuté (toutes les cases cochées) mais jamais commité (CSS, gabarits FR, page d'accueil, logo, SVG éditoriaux, `scripts/tests/ui-polish-test.php`).
- 12/12 scripts de test passent (`scripts/tests/*`, `plugins/lead-platform-connector/tests/*`).
- **Aucun projet Dokploy n'existe pour souvara.fr** : le site n'est déployé nulle part. `docker-compose.dokploy.yaml` et `Dockerfile.dokploy` sont prêts.
- `docs/launch/open-items.md` : tous les blocants juridiques et de configuration sont non cochés. La page `/page/confidentialite` affiche publiquement « À compléter avant ouverture publique ».

## Hallmark audit — page d'accueil

### Critical

1. **Gradient headline** — `index.fr.html:63, :141, :302, :532` (`.sd-gradient-text`), `custom.css:382, :540` (bleu → `--sd-purple`). → Encre pleine ; au plus un span en `--color-accent`.
2. **Stamp lies** — `hallmark-redesign.css:1` annonce « Map / Diagram · Ft5 Statement ». La page réelle : hero centré → bande « stats » → grille 6 cartes → 4 cartes-étapes → split → 6 cartes → liste → bandeau CTA → FAQ → formulaire → footer colonnes. C'est la pile-gabarit IA, 9 sections, 8 `h2`. → Réduire à ~5 sections ou corriger le stamp.
3. **Icon-tile feature cards ×2** — `.sd-step` ×4 largeurs identiques 271 px (`index.fr.html:196-250`) ; `.sd-cert-card` ×6 largeurs identiques 173 px (`:323-380`). → Liste numérotée typographique pour la méthode ; une phrase pour « périmètre d'une qualification ».
4. **Proof bar sans preuve** — `.sd-stats` (`:107-135`) : quatre libellés numérotés 01–04 dans une bande sombre ; squelette d'une barre de métriques dont on a retiré les chiffres. → Supprimer (la légende du hero couvre déjà le sujet).
5. **Page interminable** — 9 331 px (desktop), 14 132 px (375 px). Sept CTA vers deux destinations avec six libellés différents (« Évaluer mes dépendances », « Décrire mon besoin », « Faire le diagnostic », « Demander un cadrage », « Discuter de mon projet », « Poser une question »). → Un verbe primaire, un secondaire.
6. **DOM mort** — 15 SVG étoiles `hidden` dans `.sd-decision-rule` (`:397-403`, `:421-427`, `:445-451`). → Supprimer.
7. **Deux feuilles de style en conflit** — `custom.css` (1 513 lignes, 83 hex, variables purple/mint/orange, halos radiaux `:291-331`, Georgia `:904`, police système `:41`) puis `hallmark-redesign.css` (1 211 lignes) qui ré-écrit chaque règle. → Une seule feuille sur la couche tokens ; retirer l'héritage.

### Major

8. **Hero centré + eyebrow désaligné** — `.sd-hero-context` en mono 12,8 px aligné à gauche au-dessus d'un bloc centré (`hallmark-redesign.css:455-463`). H1 76,8 px, 60 caractères, 4 lignes. → Biais gauche, H1 ≤ 50 caractères ou `--text-display-s`.
9. **Eyebrow pilule avec point** — `:191` « ● Notre méthode ». → Supprimer ; la section est ordinale, un numéro suffit.
10. **Grille CSS posée sur des `col-*` Bootstrap** — `.sd-path-index > [class*="col-"]` (`hallmark-redesign.css:688-770`). La première carte est un bloc marine de 502 px dont 60 % est vide. → Vraie grille ou liste ; pas de slab décoratif.
11. **Styles inline improvisés** — `index.fr.html:451-456, :523-529` (`font-size: clamp(...)`, `font-weight: 800`, `color: var(--sd-muted)`). → Tokens.
12. **Nav N1** (wordmark + 4 liens + CTA droite, sticky, filet). Toléré en modern-minimal, mais c'est l'empreinte la plus reconnue. → Au minimum retirer « Accueil ».
13. **Footer** brand + 2 colonnes + ligne copyright : Ft3 allégé, pas le Ft5 annoncé.
14. **Check-lists vertes ×2** — `.sd-feature-list-icon` ×4 (`:280-310`) et `.sd-form-side-list` ×4 (`:537-555`). → Une seule, ou des `<dl>` sans icônes.
15. **Icônes à voix mixte** — SVG inline avec `stroke-width` 2 / 2,5 / 3 ; glyphe texte « ISO » dans un cercle (`:333`). → Une bibliothèque, un trait.
16. **Rythme uniforme** — chaque `.sd-section` a le même padding, bandes teintées en alternance.

### Minor

17. Apostrophes droites mêlées aux `&rsquo;` (`:139, :304, :446, :460, :483…`).
18. Sept SVG flèche identiques copiés-collés dans les boutons ; à factoriser en classe.

**Bilan — 7 critical · 9 major · 2 minor. Verdict : lit comme généré par IA (structure), même si la couche tokens/typo est saine.** Le rendu mobile passe le plancher (pas de défilement horizontal, boutons sur une ligne).

## Cohérence de la copie (vs. brief du 31 août)

- **Terme pivot.** Le site dit « dépendances numériques » / « indépendance numérique » ; « souveraineté numérique » n'apparaît que dans `meta keywords` et une question FAQ. Le brief veut la souveraineté comme résultat. Décision à prendre : souveraineté = objectif, indépendance = chemin.
- **Offre propre absente.** Aucune section « ce que nous faisons » (conseil, accompagnement). Aucune mention de la plateforme d'agents IA (grep : 0 occurrence). L'à-propos mentionne « conseil » dans « comment le site est financé » seulement.
- **Partenaires.** Disclosure présente (`/page/transparence-partenariats`, cas AIFEL). Absent : « le partenaire est choisi pour l'adéquation au cas d'usage, pas pour le partenariat » sur l'accueil ; absent partout : « les partenaires participent au financement / subvention du projet ».
- **Écosystème américain.** Nommé une fois (« outils américains », `/page/independance-numerique`) et dans une FAQ. Le hero ne dit pas de quoi on sort.
- **Une chose, cinq noms.** Nav « Décrire mon besoin » → page « Demande de cadrage » → H1 « Décrire vos dépendances » → footer « Diagnostic » → section « Formulaire de cadrage ». → Un seul nom : Diagnostic.
- **Texte de chantier public.** `/page/confidentialite` : « À compléter avant ouverture publique », « Version de travail ». Blocant.
- Lexique IA : faible. Phrases courtes, vouvoiement stable, dates de revue présentes. Quelques parallélismes négatifs (« pas de drapeau », « sans déplacer le risque ailleurs ») — acceptables.

## Formulaires

Trois formulaires différents pour une même file :

| Emplacement | Champs | Obligatoires |
|---|---|---|
| Accueil `#contact` | 12 `input type=text` (dont « Message » sur une ligne, « Budget », « Effectif », « Secteur » en texte libre) | nom, e-mail, **téléphone** |
| `/page/contact` | mêmes 12 | idem |
| `/page/diagnostic-souverainete` | nom, e-mail, organisation, fonction, textarea | nom, e-mail, organisation, message |

Le backend (`submit.php`, `lead-form.20260827.js`) sérialise tout champ présent, gère le consentement nominatif (`aifel` / `provider-intro-v1`) et la file chiffrée : un formulaire multi-étapes n'exige pas de refonte, seulement (a) une sauvegarde partielle à l'étape 1 et (b) des champs structurés.

### Proposition — un composant, trois étapes

- **Étape 1 — contact (sauvegardée immédiatement, sert au remarketing)** : e-mail professionnel, nom, organisation, fonction (select : DSI · RSSI · CTO · DG/DGS · achats · transformation · autre).
- **Étape 2 — qualification** (champs du spec §10) : type d'organisation (PME · ETI · grand compte · collectivité · établissement public · santé · autre) ; effectif (tranches) ; outils en place (cases : Microsoft 365 · Teams · Google Workspace · Zoom · AWS · Azure · GCP · autre) ; cas d'usage examinés (visio/collab · messagerie · bureautique · identité · hébergement · sauvegarde · IA/agents · autre) ; contraintes (données sensibles/RGPD · HDS · NIS2 · SecNumCloud · commande publique) ; déclencheur (renouvellement · incident · audit · programme cloud · exigence de tutelle) ; échéance (< 3 mois · 3–6 · 6–12 · > 12) ; budget indicatif (tranches, facultatif) ; **intérêt pour un financement ou une subvention via partenaires** (oui/non).
- **Étape 3 — suite souhaitée** : résultat indépendant seul · échange de 30 min · accompagnement · mise en relation nominative (fait apparaître la case de consentement partenaire) ; message libre ; téléphone (facultatif).

## Séquence recommandée

1. Commiter le travail UI-polish non commité (testé), pousser la branche, PR vers `master`. Ne pas déployer.
2. Figer la plateforme de copie (terme pivot, explicitation de l'écosystème US, les trois voies : conseil / plateforme agents IA / partenaires + financement, nom unique « Diagnostic »).
3. Refonte de l'accueil sur place, 5 sections, une feuille CSS, encre pleine, listes typographiques.
4. Composant d'entrée unique en 3 étapes avec sauvegarde partielle ; remplacer les trois formulaires.
5. Lever les blocants de `open-items.md`, créer le projet Dokploy, DNS + TLS, relecture manuelle des contenus, fusion dans `master`, déploiement.
