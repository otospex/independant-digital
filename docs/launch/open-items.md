# Entrées externes requises avant ouverture publique

Le code, les contenus et la file de demandes peuvent être testés en préproduction. Les formulaires ne doivent pas être ouverts au public tant que les éléments ci-dessous ne sont pas renseignés dans la notice de confidentialité et les mentions légales.

## Blocants juridiques

- [ ] **Identité juridique** — l&rsquo;exploitant retenu est **Otospex Solutions SARL** (17 avenue Bourguiba, 4180 Houmet Souk, Tunisie ; +216 28 87 01 85 ; contact@otospex.com), RNE 1821140M, capital 105 000 TND, renseigné dans `/mentions-legales` et `/confidentialite` (seed, 2026-09-24). Reste un marqueur `[À compléter]` : le nom du gérant (directeur de la publication, `/mentions-legales`).
- [ ] **Représentant dans l&rsquo;UE (art. 27 RGPD)** — décision du 2026-09-24 : lancement sans représentant, les deux pages indiquent « désignation en cours » et renvoient vers l&rsquo;éditeur. Prévu : Accretio. À la désignation, remplacer ce paragraphe dans `/confidentialite` et `/mentions-legales` par le nom et l&rsquo;adresse du représentant, puis rejouer le seed ou mettre la base à jour.
- [x] **Contact données personnelles** (2026-09-24) — `contact@otospex.com` (objet « Données personnelles »), l&rsquo;adresse déjà publiée par otospexsolutions.com ; une boîte dédiée peut la remplacer plus tard sans autre changement.
- [x] **Hébergeur** (2026-09-24) — Verpex Limited, n° 12106087 (Angleterre et pays de Galles), siège Evolution House, Iceni Court, Delft Way, Norwich NR6 6BB, Royaume-Uni (registre Companies House, vérifié le 2026-09-24 ; les pages verpex.com se contredisent), +44 20 3095 4271, serveur `s9513.fra1.stableserver.net` = Francfort-sur-le-Main. Renseigné dans les deux pages.
- [x] **Durée de conservation** (2026-09-24) — 12 mois sans suite, 36 mois après mise en relation (cohérent avec la politique de confidentialité d&rsquo;Otospex : contacts jusqu&rsquo;à 3 ans). Reporté dans `/confidentialite`. Purge planifiée le 2026-09-26 : cron quotidien `LEAD_RETENTION_DAYS=365 … scripts/purge-leads.php --apply` (`deploy/verpex/crontab`, journal `storage/logs/cron.log`).
- [ ] **Mentions légales** — page `/mentions-legales` créée et liée depuis le pied de page ; restent quatre marqueurs `[À compléter]` (capital social, RNE / matricule fiscal, nom du gérant, représentant UE) — voir « Identité juridique ». Transferts hors UE : la notice s&rsquo;appuie sur les clauses contractuelles types (décision 2021/914) pour l&rsquo;accès de l&rsquo;équipe depuis la Tunisie — les signer et les tenir à disposition.

## Configuration de production

- [x] **Domaine et TLS** (2026-09-26) — `souvara.fr` sert le site sur Verpex (certificat AutoSSL couvrant souvara.fr, www et independantdigital.fr, renouvelé automatiquement) ; `www.souvara.fr`, `independantdigital.fr` et HTTP redirigent en 301 vers `https://souvara.fr`. Racine web = `public/` (voir `deploy/README.md`).
- [ ] Créer l&rsquo;adresse de contact publiée et tester sa délivrabilité. Ne pas afficher une adresse tant que sa boîte et ses enregistrements DNS ne sont pas opérationnels. — 2026-09-26 : boîtes `contact@souvara.fr` et `leads@souvara.fr` créées (MX, SPF, DKIM, DMARC en place, livraison locale vérifiée) ; reste un envoi depuis une adresse externe vers `contact@souvara.fr`.
- [x] **Copie e-mail des demandes** (2026-09-26) — chaque demande aboutie est envoyée à `leads@souvara.fr` (Reply-To = le visiteur), les diagnostics abandonnés par le cron horaire ; la liste admin des demandes déchiffre nom, e-mail, téléphone, entreprise et réponses. Vérifié de bout en bout sur souvara.fr.
- [ ] Sauvegarder la clé `storage/lead-platform-connector.key`. Sans elle, les demandes chiffrées en attente ne sont plus lisibles.
- [ ] Définir et tester la sauvegarde de la base contenant `lead_submission` ainsi que la procédure de restauration.
- [ ] Configurer le système de distribution de leads en remplissant ensemble `platform_url` et la clé API. Laisser les deux champs vides maintient la file locale; n&rsquo;en remplir qu&rsquo;un bloque les envois.
- [x] Tâche de purge : `scripts/purge-leads.php` (2026-09-02). Reste à la planifier (cron) avec la durée retenue.
- [x] Clés `site.settings.description` résolues dynamiquement (`@desc_en_id` / `@desc_fr_id`) dans `deploy/seed.sql` (2026-09-02).
- [x] **Sitemaps et robots** (2026-09-02) — `/sitemap.xml` (index), `/sitemap-pages.xml`, `/sitemap-posts.xml`, `/sitemap-solutions.xml` générés par `app/controller/sitemap.php` et le plugin annuaire, `<loc>` absolus sur `CANONICAL_URL`, hreflang + x-default. `robots.txt` déclare le sitemap en absolu et autorise explicitement les robots IA (GPTBot, ClaudeBot, PerplexityBot, Google-Extended, CCBot…). nginx : `deploy/docker/nginx.conf` (conf de l&rsquo;image + location sitemap) copié par `deploy/docker/Dockerfile`. À faire après mise en ligne : soumettre `https://souvara.fr/sitemap.xml` dans Search Console et Bing Webmaster.
- [x] **URLs sans préfixe `/page/`** (2026-09-02) — pages à `/{slug}`, articles à `/blog/{slug}`, anglais sous `/en/…` ; `/page/{slug}` et `/{lang}/page/{slug}` répondent 301. Liens réécrits dans les gabarits, le seed et la base.
- [x] **Canonicals par page** (2026-09-02) — `link[rel=canonical]`, `og:url`, `og:title`, `og:description` calculés par page (`CANONICAL_URL` + chemin) via `common.tpl` / `post.tpl` après la propagation du `<head>`. Vérifier `CANONICAL_URL=https://souvara.fr` dans l&rsquo;environnement Dokploy (déjà dans `deploy/docker/docker-compose.yaml`).
- [x] **Overlay de déploiement complet** (2026-09-02) — `deploy/docker/Dockerfile` copie désormais `config/app-routes.php`, `env.php`, le plugin `solutions-directory` et ses assets, le contrôleur sitemap, robots, les gabarits et `scripts/` ; `scripts/tests/deploy-overlay-test.php` échoue dès qu&rsquo;un fichier du fork n&rsquo;est pas couvert. Avant ce correctif, l&rsquo;annuaire et ses routes n&rsquo;atteignaient jamais la production.
- [ ] **Recette de cache au déploiement.** Après toute modification de routes, de PHP ou de gabarits, purger dans cet ordre puis redémarrer : `rm -f storage/compiled-templates/app_1_*` ; `rm -rf public/page-cache/* storage/cache/routes.* storage/cache/vvveb.plugins_list_*` ; `docker compose restart php`. Omettre `storage/cache/routes.*` laisse tourner l&rsquo;ancienne table de routage ; omettre `vvveb.plugins_list_*` laisse tourner l&rsquo;ancienne liste de plugins. Les entrées `public/page-cache/*/page/*` de l&rsquo;ancienne forme d&rsquo;URL doivent disparaître (le déploiement vide `page-cache` à chaque démarrage).
- [x] **Flush single-flight** (2026-09-02) — `scripts/flush-partial-leads.php` prend un `flock` non bloquant (`storage/flush-partial-leads.lock`) et sort en 0 si une exécution est en cours. La base doit tourner en UTC ; le script avertit sur STDERR si l&rsquo;horloge de la base s&rsquo;en écarte.

## Validation commerciale et éditoriale

- [ ] AIFEL fournit les preuves demandées dans `docs/providers/aifel/evidence-request.md`; la revue passe explicitement à publiable avant toute fiche ou formulaire nominatif.
- [ ] Les pages d&rsquo;alternatives restent avec `editorial_ready=0` jusqu&rsquo;à saisie d&rsquo;une source de demande française et validation de la checklist manuelle.
- [ ] **Suivi et consentement** (plugin `site-tracking`, 2026-09-03) — réglages dans l&rsquo;admin : Extensions → « Suivi et consentement » (`/admin/index.php?module=plugins/site-tracking/settings`). Trois zones : Matomo (URL + idSite, tag sans cookie donc exempté de consentement ; idGoal optionnel déclenché à chaque demande envoyée), scripts exemptés chargés dans le `<head>`, tags marketing (GTM, Google Ads, Meta…) stockés inertes et exécutés seulement après acceptation dans le bandeau (choix conservé six mois, lien « Cookies » du pied de page pour changer d&rsquo;avis). Événements pour les tags : `id:track` (DOM) et `dataLayer.push({event:'id_lead'})`. Reste : installer Matomo (compte Verpex ou Matomo Cloud UE), renseigner l&rsquo;URL et l&rsquo;idSite, créer l&rsquo;objectif « lead », compléter la notice de confidentialité avec les traceurs réellement déployés. Vérification à faire après déploiement : le tag apparaît dans le `<head>` d&rsquo;une page publique et le bandeau n&rsquo;apparaît que si un tag marketing est renseigné.
- [ ] `/blog` figure dans la navigation et reste vide tant que le publieur programmé n&rsquo;a pas tourné. Soit lancer la publication programmée avant l&rsquo;ouverture, soit retirer l&rsquo;entrée de la navigation : une rubrique annoncée puis vide est un défaut visible dès la première visite.
- [ ] La meta description de la page d&rsquo;accueil anglaise dans `deploy/seed.sql` (~ligne 2198) affirme un cloud souverain « SecNumCloud-certified » : à réécrire avant toute exposition publique de `/en/`, cette certification n&rsquo;étant pas établie côté commercial/éditorial.

## Décision d&rsquo;ouverture

Le feu vert est consigné avec la date, le nom du responsable et les URL des notices finales. Une case cochée doit renvoyer vers une preuve ou une configuration vérifiée; elle ne doit pas reposer sur une hypothèse.

- [x] **Balises canonical/og inertes** — réglé par le calcul par page dans la propagation (voir ci-dessus). Les balises restées dans `generated/*.fr.html` sont sans effet et peuvent être nettoyées à l&rsquo;occasion.
- [ ] **Pages de démonstration anglaises publiées** — `/en/marketplace`, `/en/pricing`, `/en/portfolio`, `/en/shipping-delivery`, `/en/subscription-policy` (restes Vvveb) sont `publish` et figurent donc dans `/sitemap-pages.xml`. Les passer en brouillon ou les supprimer avant soumission du sitemap.
- [ ] **Annuaire : six fiches déclarées** (Tixeo, Oodrive Work, BlueMind, OUTSCALE, Olvid, Wimi) seedées le 2026-09-02 en `verification_status=declare`. Avant tout passage en « vérifié » : revoir chaque fiche avec la checklist, confirmer les dates de validité au catalogue ANSSI (CSPN Tixeo expirée le 24/01/2026, CSPN Olvid « non maintenu », SecNumCloud Outscale à renouveler le 30/11/2026) et l&rsquo;hébergeur d&rsquo;Olvid.
- [x] **Audit SEO, correctifs techniques** (2026-09-02, après-midi) — `/fr/…` en 301, pages anglaises `noindex` hors sitemap, `/annuaire` hub liant chaque catégorie et chaque page « alternative à », titres et descriptions propres sur les pages de terme, derniers liens `/page/…` réécrits, JSON-LD sans entités HTML, `robots.txt` fermant `/cart/`, `/checkout/`, `/page-cache/`. Crawl : 51 URL, zéro erreur.
- [ ] **Visuels de l&rsquo;annuaire** (2026-09-02) — les six logos sous `public/media/solutions/{slug}/` ont été récupérés sur les sites officiels des éditeurs (Oodrive et OUTSCALE : version blanche recolorée en encre) : à remplacer par les fichiers des kits presse. Aucune capture d&rsquo;écran encore : renseigner la méta `screenshots` (une ligne par image, `media/solutions/{slug}/fichier.png | légende`) uniquement avec des images dont les droits sont confirmés. Les liens soumis par le formulaire (`submitted_logo_url`, `submitted_screenshot_urls`) restent privés jusqu&rsquo;à téléversement par un éditeur.
- [x] **Corpus réglementaire publié** (2026-09-03) — onze pages relues et publiées : souveraineté numérique, cloud souverain, cloud de confiance, CLOUD Act, RGPD et hébergement, NIS2, SecNumCloud, HDS, secteur public, comparatif des clouds, étapes de migration. Relecture : liens `/fr/` et astérisques Markdown corrigés, JSON-LD aligné sur les slugs réels, promesse « 2 minutes » retirée, nuance Data Privacy Framework (RGPD, CLOUD Act), « immunité » SecNumCloud reformulée en critères de protection, plafonds de sanction et transposition NIS2, référentiel HDS 2024, circulaires « Cloud au centre », tableau comparatif renvoyant au catalogue ANSSI, sources primaires et date de revue sur chaque page. Entrées ajoutées au menu Guides et au hub `/independance-numerique`. Prochaine revue : mars 2027.
- [ ] **Illustrations des guides** — les images `media/generated/*.jpg` sont des visuels de remplissage : un même fichier servait de hero et d'illustrations intercalées (retirées), et cinq pages partagent la même image. À remplacer par des visuels propres à chaque page.
- [ ] **Titres corrompus dans le seed** — les brouillons non publiés (`cloud-souverain`, `certifications`, `conformite-audit`, `strategie-conseil`, `formation`, `cybersecurite-soc`, `protection-donnees`, `solutions`, `barometre-souverainete-numerique`, `estimateur-cout-migration`…) portent des caractères de remplacement (�) dans `name` / `meta_description` de `deploy/seed.sql`. À corriger avant toute publication de ces pages.
- [ ] **Audit SEO, reste côté contenu** — voir `docs/launch/seo-audit-2026-09-02.md` §4 : trancher le blog, étoffer les intros de termes, page financement, titres trop longs ou sans marque.
