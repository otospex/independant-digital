# Audit SEO technique et sémantique — 2 septembre 2026

Périmètre : la prévisualisation locale (`http://127.0.0.1:8090`) de la branche
`codex/french-growth-foundation` après la passe d’infrastructure du même jour
(URLs sans `/page/`, canonicals par page, sitemaps automatiques, robots,
annuaire seedé). Les URL absolues portent l’hôte de prévisualisation parce que
`CANONICAL_URL` y pointe ; en production il vaut `https://souvara.fr`.

## 1. Infrastructure vérifiée

| Point | État | Preuve |
|---|---|---|
| Pages à `/{slug}`, articles à `/blog/{slug}`, anglais sous `/en/` | En place | `scripts/tests/url-structure-test.php` (statique + live) |
| `/page/{slug}` et `/{lang}/page/{slug}` | 301 vers la nouvelle forme, sur l’origine canonique | idem, `app/controller/content/legacy-page.php` |
| Canonical auto-référent, `og:url`, `og:title`, `og:description` par page | En place | `common.tpl`, `post.tpl`, test live |
| `/sitemap.xml` (index) → `/sitemap-pages.xml`, `/sitemap-solutions.xml` (`/sitemap-posts.xml` dès qu’un article est publié) | 200, XML valide, `<loc>` absolus, hreflang + `x-default` | `scripts/tests/sitemap-test.php` |
| `robots.txt` | Sitemap absolu ; GPTBot, ClaudeBot, PerplexityBot, Google-Extended, CCBot, OAI-SearchBot, Applebot-Extended… explicitement autorisés ; `/admin/`, `/user/`, `/search/` fermés | `public/vrobots.txt`, `app/controller/feed/robots.php` |
| nginx production : `/sitemap*.xml` atteint PHP en 200 | `nginx.dokploy.conf` copié dans l’image | `Dockerfile.dokploy`, test overlay |
| Page d’inscription à l’annuaire hors sitemap (noindex) | Exclue | `Sitemap::EXCLUDED_PAGE_SLUGS` |
| Overlay de déploiement complet (l’annuaire n’atteignait pas la production) | Corrigé, image construite | `scripts/tests/deploy-overlay-test.php` |
| Maillage interne | Nav + pied de page sur tous les gabarits ; annuaire lié depuis l’accueil, les guides embarquent le bloc annuaire, les fiches se lient entre elles (Alternatives) | `scripts/sync-french-chrome.php`, seed |

## 2. Audit technique (crawl de la prévisualisation)

Crawl du 2 septembre 2026 (après-midi) : 51 URL HTML découvertes depuis
`/sitemap.xml` puis par suivi des liens internes. Aucune erreur HTTP, aucune
chaîne de redirection, canonical auto-référent sur toutes les pages indexables.

| Constat | Périmètre | Correction |
|---|---|---|
| Les pages de terme (`/annuaire/categorie/…`, `/annuaire/alternative-a/…`) servaient le `<title>` et la description de l’accueil | 26 URL | Corrigé : le contrôleur fournit titre, description et `og:title` propres, et force `content/post.tpl` (le nom de gabarit suffixé `.fr.html` faisait retomber la vue sur `common.tpl`). Test live dans `url-structure-test.php`. |
| `/fr/{slug}` répondait 200 en doublon de `/{slug}` | toutes les pages | Corrigé : 301 vers la forme sans préfixe (`app/controller/base.php`). |
| `/sitemap-pages.xml` listait les pages anglaises alors que leurs gabarits sont `noindex` | 11 URL `/en/` | Corrigé : la langue `en` est exclue du sitemap ; sans seconde langue indexable, plus d’alternates hreflang. |
| `/annuaire` figurait dans les deux sitemaps | 1 URL | Corrigé : réservé au sitemap de l’annuaire. |
| Pages de catégorie et d’alternative atteignables uniquement par un `<select>` ou les puces d’une fiche (orphelines pour un robot) | 26 URL | Corrigé : `/annuaire` liste désormais toutes les catégories et toutes les pages « Alternatives à » ; entrées 2 à 5 liens entrants chacune. |
| Liens internes vers l’ancienne forme `/page/…` (contact depuis les fiches, confidentialité depuis le formulaire d’inscription, guides depuis les intros de termes) | 3 gabarits + données de termes | Corrigé dans `plugins/solutions-directory/config.php`, `solution-presenter.php`, `term-data.php`. |
| JSON-LD contenant des entités HTML (`&eacute;`) donc invalide | accueil + 14 gabarits générés | Corrigé : échappement `\uXXXX`. |
| `robots.txt` laissait `/cart/`, `/checkout/`, `/page-cache/` ouverts | site | Corrigé. |
| `/blog` (archive vide) reprend le titre et la description de l’accueil | 1 URL | **Ouvert.** Hors sitemap, donc sans effet immédiat, mais un titre dupliqué dès qu’un robot suit la navigation. Se règle par la décision blog (§5). |
| Titres au-delà de 65 caractères (tronqués dans les SERP) | `/annuaire/alternative-a/microsoft-teams`, `google-workspace`, `microsoft-azure`, `chatgpt-openai` ; `/solution/outscale`, `/solution/bluemind` | **Ouvert, mineur.** Raccourcir le suffixe « — Annuaire des solutions souveraines » ou les titres de fiche. |
| Titres trop courts / sans marque | `/contact` (« Nous contacter »), `/mentions-legales`, `/solution/oodrive-work` | **Ouvert, mineur.** Ajouter le nom du site en suffixe. |
| Descriptions sous 70 caractères | `bureautique`, `fichiers-et-partage`, `sauvegarde` | **Ouvert, mineur.** Étoffer l’intro de ces trois termes. |
| Logo de navigation avec `alt=""` | toutes les pages | Conforme : image décorative, le lien porte un `aria-label`. |
| Page d’inscription sans JSON-LD | 1 URL | Conforme : page `noindex,follow`. |

Profondeur : toutes les pages de contenu sont à un clic de l’accueil ; les
fiches à deux clics ; les termes à deux clics via `/annuaire`.

## 3. Audit sémantique / entités

Grille : intention de recherche servie, délai d’accès à la réponse (« time to
retrieval »), complétion de l’objectif (CTA, étape suivante), couverture des
entités attendues par un DSI/RSSI.

### Ce qui est servi

- **Accueil, diagnostic, méthode, transparence** : l’intention « comprendre la démarche et engager un diagnostic » est servie ; CTA vers `/diagnostic-souverainete` et `/contact` présents sur chaque gabarit (deux à trois liens par page), formulaire d’admission dans le hero.
- **Fiches de solution** (600 à 870 mots) : hébergement, qualification ANSSI avec dates, tarification, limites, relation commerciale. C’est le contenu le plus dense en entités (SecNumCloud, ANSSI, HDS, ISO 27001, hébergeur, chiffrement) et le seul qui cite des sources.
- **Guides publiés** (`/sortir-microsoft-365`, `/choisir-visioconference-collaboration`, `/independance-numerique`) : 415 à 600 mots, intention « remplacer X » servie, mais une seule mention de SecNumCloud et aucune de RGPD, Cloud Act, NIS2, CNIL, réversibilité.
- **Pages de terme** (230 à 350 mots) : listes de fiches avec intro d’une phrase. Suffisant pour un hub, insuffisant pour se positionner sur « alternative à Zoom » ou « cloud souverain ».

### Ce qui manque

- **Le corpus réglementaire n’est pas publié.** Les seules pages qui expliquent Cloud Act, NIS2, RGPD et hébergement, SecNumCloud, cloud de confiance, HDS et secteur public existent en brouillon (`cloud-act-france`, `directive-nis2-conformite`, `rgpd-hebergement-donnees`, `secnumcloud-qualification-anssi`, `cloud-de-confiance`, `hebergement-donnees-de-sante-hds`, `souverainete-numerique-secteur-public`, `souverainete-numerique-guide`, `migration-cloud-souverain-etapes`, `comparatif-cloud-souverain`) et dans `generated/*.fr.html`. Sur les pages publiées, comptage hors navigation : Cloud Act 0, FISA 0, NIS2 1 (diagnostic), RGPD 1, CNIL 0, DORA 0, EUCS 0, Schrems 0, clauses contractuelles types 0. Un DSI ou RSSI cherchant l’une de ces entités ne trouve rien à indexer.
- **Le financement n’est traité que par l’accueil et la page transparence** (une mention chacune) ; aucune page ne nomme France 2030, Bpifrance ou un dispositif régional, alors que le brief en fait un des trois leviers.
- **Les concurrents nommés** (Microsoft, Google, AWS, Zoom, Slack, Notion, Salesforce, OpenAI) n’apparaissent que dans les slugs et titres de termes ; aucun contenu comparatif ne les décrit.
- **Les trois pages programmées** (`alternatives-microsoft-teams`, `alternative-zoom-francaise`, `suite-collaborative-francaise`) répondraient à des requêtes « alternative à », mais restent `scheduled` tant que le publieur programmé n’a pas tourné.
- **Délai d’accès à la réponse** : correct sur les fiches (le premier `<h2>` est « Ce que la solution propose »), long sur les guides dont le premier écran est un préambule de positionnement avant la première recommandation.

## 4. Priorités

1. ~~**Publier le corpus réglementaire**~~ **Fait le 3 septembre 2026** : les onze pages sont relues, sourcées, datées et publiées, liées depuis le menu Guides, le hub `/independance-numerique`, et vers l’annuaire (catégorie ou fiche pertinente). Voir `open-items.md` pour ce qui reste (illustrations).
2. **Trancher le blog** : lancer le publieur programmé (quatre articles + trois pages « alternative ») ou retirer « Blog » de la navigation. Règle du même coup le titre dupliqué de `/blog`.
3. **Étoffer les intros de termes** à trois ou quatre phrases (contexte, critères, lien vers le guide) : 26 pages passent de « liste » à « réponse », et les trois descriptions courtes disparaissent.
4. **Une page financement** nommant les dispositifs réellement mobilisables, liée depuis le diagnostic et les fiches.
5. **Réglages mineurs** : titres > 65 caractères (6 URL), titres sans marque (3 URL). À faire lors de la relecture manuelle, sans priorité.

Après mise en ligne : soumettre `https://souvara.fr/sitemap.xml` dans
Search Console et Bing Webmaster, vérifier que `CANONICAL_URL` vaut bien
l’origine de production, et relancer ce crawl sur le domaine.

## 5. Reste côté contenu (hors code)

- Renseigner les marqueurs `[À compléter]` de `/mentions-legales` et `/confidentialite` (capital, RNE, gérant, représentant UE, hébergeur, adresse données personnelles, durées de conservation).
- Passer en brouillon les pages de démonstration anglaises encore publiées (`/en/marketplace`, `/en/pricing`, `/en/portfolio`, `/en/shipping-delivery`, `/en/subscription-policy`) ou les retirer.
- Décider du blog : publier les quatre articles FR en brouillon (ou retirer l’entrée « Blog » de la navigation) ; `/sitemap-posts.xml` apparaîtra automatiquement dans l’index dès le premier article publié.
- Relire les six fiches de l’annuaire avec la checklist avant tout passage en « vérifié ».
